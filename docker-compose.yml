version: '3'
services:
  todo:
    build: .
    volumes:
      - ./DockerCode : /app/
    ports:
      - 3000:80
  web:
    image: php:apache
    volumes:
      - ./DockerCode : /var/www/html
    ports:
      - 3001:80
    depends_on:
      - todo